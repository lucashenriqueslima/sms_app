import 'package:flutter/material.dart';

class AmostragemFormPage extends StatelessWidget {
  const AmostragemFormPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
